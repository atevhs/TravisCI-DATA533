language: python
python:
  - 2.7
  - 3.5
  - 3.6
before_install:
  - python --version
  - pip install -U pip
  - pip install -U pytest
  - pip install pygame
  - pip install os
  - pip install sys
  - pip install time
  - pip install codecov
script:
  -python Test_suite.py
